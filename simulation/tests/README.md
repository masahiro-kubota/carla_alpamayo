# Simulation Scenario Tests

`simulation/tests/` は、`CARLA` 結合シナリオテストの定義と期待値をまとめて管理する場所です。

現在の suite:

- [stopped_obstacle/README.md](/home/masa/carla_alpamayo/simulation/tests/stopped_obstacle/README.md)

設計メモは `docs/` に残し、実際に回す run-config と期待値チェックは `simulation/` 配下に寄せる。
